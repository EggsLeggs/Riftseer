# Railway config: use Docker and bun (not npm) so the container starts correctly.
# The Dockerfile sets WORKDIR to packages/api so the static plugin finds ./public.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "bun src/index.ts"
healthcheckPath = "/api/health"
