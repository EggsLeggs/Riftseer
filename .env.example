# ─── Runtime ───────────────────────────────────────────────────────────────────
# Which provider to use: "riftcodex" | "riot"
CARD_PROVIDER=riftcodex

# Path to the SQLite database file (relative to cwd or absolute)
DB_PATH=./data/riftseer.db

# Log level: "debug" | "info" | "warn" | "error"
LOG_LEVEL=info

# ─── RiftCodex Provider ────────────────────────────────────────────────────────
RIFTCODEX_BASE_URL=https://api.riftcodex.com
# Optional API key (not currently required)
# RIFTCODEX_API_KEY=

# How often to refresh the card cache (ms). Default: 6 hours
CACHE_REFRESH_INTERVAL_MS=21600000

# Upstream request timeout (ms)
UPSTREAM_TIMEOUT_MS=15000

# Fuzzy match threshold for fuse.js (0 = exact, 1 = anything). Default: 0.4
FUZZY_THRESHOLD=0.4

# ─── Elysia API ────────────────────────────────────────────────────────────────
API_PORT=3000

# Public-facing URL of this API (used in bot reply links)
API_BASE_URL=http://localhost:3000

# Future React site URL (used in bot reply links)
SITE_BASE_URL=https://example.com

# ─── Discord Bot (packages/discord-bot) ────────────────────────────────────────
# Set these as Cloudflare Workers secrets: wrangler secret put <name>
DISCORD_PUBLIC_KEY=    # Ed25519 public key from Discord Developer Portal
DISCORD_BOT_TOKEN=     # Bot token from Discord Developer Portal → Bot
DISCORD_APPLICATION_ID= # App ID from Discord Developer Portal → General Information
# Public vars go in packages/discord-bot/wrangler.toml (API_BASE_URL, SITE_BASE_URL)

# ─── Reddit Bot (packages/reddit-bot) ──────────────────────────────────────────
# Create a Reddit script app at https://www.reddit.com/prefs/apps
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USERNAME=
REDDIT_PASSWORD=
# User-Agent string (Reddit requires a unique UA)
REDDIT_USER_AGENT=RiftSeer/0.1.0 by u/YourUsername

# Comma-separated list of subreddits to watch (no r/ prefix)
REDDIT_SUBREDDITS=riftbound

# How many comments/posts to fetch per poll
REDDIT_POLL_LIMIT=100

# Polling interval in ms. Default: 30s
REDDIT_POLL_INTERVAL_MS=30000
