/**
 * Re-export of the canonical card model.
 *
 * A blanket re-export on purpose: an explicit list here was a second copy of
 * the type surface that had to be edited every time `@riftseer/types` gained a
 * member, and it silently went stale when nobody did.
 */
export * from "@riftseer/types";
